# maintenance

Enable or disable WordPress maintenance mode

## Usage

```bash
dvs maintenance [action]
```

## Argument

- `action` - command to choose from:
    - `status`: Shows the active/inactive status
    - `enable`: Enables WordPress maintenance mode
    - `disable`: Disables WordPress maintenance mode
