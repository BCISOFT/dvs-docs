# dvs changelog

Display the list of changes by version.

## Description

The `changelog` command displays the list of versions with details of changes and new features:

- displays the complete list of versions if the current version is up to date
- displays only new features with the next update

## Usage

```bash
dvs changelog
```
