# dvs login

Manage Docker registry connection for DVS.

## Description

The `login` command authenticates with the Docker registry configured for your DevSpaces installation and allows access to additional features if your license permits it.

## Usage

```bash
dvs login
```

## Usage

```bash
# Connect to registry
dvs login
```

## Notes

Mainly useful to force a login if automatic connection fails.

