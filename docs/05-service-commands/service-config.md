# dvs service config

Configure a service by editing its configuration file.

## Usage

```bash
dvs service config [service]
```

The command opens an editor (nano) to modify the service configuration file. If the configuration file is modified, the service is automatically restarted.

See the list of available services on [the service page](/docs/service-commands/).


