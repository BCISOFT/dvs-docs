# dvs tunnel status

Affiche les informations sur le tunnel pour l'application active. Ces informations sont aussi affichées sur `dvs info` lorsque le tunnel est actif.

## Usage

```bash
dvs tunnel status
```
